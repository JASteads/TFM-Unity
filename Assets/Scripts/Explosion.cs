using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Explosion : MonoBehaviour {
    // TBC
    /* public LayerMask inExplosion;
    float aoe;
    public Vector2 explosion;
    Vector2 forceAppliedX;
    Vector2 objectIn;
    Rigidbody2D obj;

    // Use this for initialization
    void Awake () {
        aoe = Vector2.Distance(explosion, objectIn);

	}
	
	// Update is called once per frame
	void OnTriggerEnter2D (Collider2D collider) {
		
        if (inExplosion == (inExplosion | 1 << collider.gameObject.layer))
        {
            forceAppliedX.Set(obj.position.x, aoe);

        }
	} */
}
